import React from 'react';
import ReactDOM from 'react-dom';

import RepositoryIndex from './Repository_Index/RepositoryIndex';

ReactDOM.render(<RepositoryIndex />, document.getElementById('root'));

